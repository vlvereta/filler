#include "./../includes/filler.h"

int		main(void)
{
	int			index;
	t_filler	*f;

	if ((f = (t_filler *)ft_memalloc(sizeof(t_filler))))
	{
		while (1)
		{
			// to do the game process ownself
		}
	}
	return (0);
}